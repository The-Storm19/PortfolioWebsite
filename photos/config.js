/* =========================================================================
   HOME PAGE BACKGROUND — PHOTO LIST
   =========================================================================
   This file controls which photos scroll behind the logo on the home
   page. It's a separate folder from "Photography" and "Graphic Design"
   on purpose — these are just background images, not portfolio entries,
   so there's no title/date/description to fill in for them.

   HOW TO ADD A BACKGROUND PHOTO
   -------------------------------
   1. Put the image file in this same "photos" folder, next to this
      config.js file.
   2. Add its file name (with extension) to the list below.
   3. Save this file and refresh the website.

   Wide, high-resolution photos work best, since they fill the full width
   of the screen. To remove a photo, delete its line. To reorder them,
   reorder the lines. If a listed file can't be found, it's simply
   skipped — the slideshow just uses whatever does load.

   The four lines below point at photos already used elsewhere on the
   site, as a placeholder — copy those image files into this "photos"
   folder (or swap in new ones of your choosing) to get the slideshow
   running.
   ========================================================================= */

window.HERO_PHOTOS = [
  "glass.1.final.schreier.Analiese.JPG",
  "IMG_7737.jpg",
  "AnalieseSchreier_AlbumCover.jpg",
  "Schreier.Analiese.Portrate.jpg"
];
